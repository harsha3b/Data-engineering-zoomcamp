
version: 2

sources:
  - name: zoomcamp_raw
    database: kestra-sandbox-499212
    schema: zoomcamp_hw_dataset
    tables:
      - name: green_tripdata
        description: "Raw green taxi trip data, loaded via Kestra pipeline."
      # add more tables here later, e.g. yellow_tripdata, if you have them